// onboarding.store.ts
import { type StateCreator } from 'zustand';
import type { OnboardingState } from "./types";

export const createOnboardingSlice :  StateCreator<OnboardingState>=(set,get)=>({

})
